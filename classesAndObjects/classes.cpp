/*Restrictions on union in C++
1. Non static member variable can not have an initializer
2.No member can be object of the class being declared, but it can have a pointer to the same class
3.It can not have a virtual function
4.No member can be declared as auto, extern, register
*/

//By default members are private
#include<iostream>

using namespace std;

